{
  "name": "timequota",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "timequota": "bun run src/cli.ts",
    "test": "bun test",
    "check": "bun test && tsc --noEmit && bun build src/cli.ts --target=bun --outdir /tmp/timequota-build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
